{
  "name": "@gigloop/e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Real-stack Playwright end-to-end suite (ADR-0048)",
  "scripts": {
    "build:stack": "sh scripts/build-stack.sh",
    "test:e2e": "npm run build:stack && playwright test",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@clerk/testing": "^1.5.0",
    "@playwright/test": "^1.49.0",
    "dotenv": "^16.4.5"
  }
}
